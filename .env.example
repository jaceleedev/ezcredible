# 상담 신청 전달처 — 비어 있으면 개발 모드에서는 서버 콘솔에만 남기고, 프로덕션에서는 "준비 중"으로 실패시킨다.
# JSON POST를 받는 주소(슬랙 웹훅, Make/n8n, Google Apps Script 등). 전송 방식이 정해지면 src/lib/deliver-consultation.ts만 바꾼다.
CONSULTATION_WEBHOOK_URL=
